import styles from "@/styles/StoreList.module.scss";

const layout = async ({ children }: { children: React.ReactNode }) => {
  return <div className={styles["store-list"]}></div>;
};

export default layout;
